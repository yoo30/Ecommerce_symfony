    <div class="well">
        <ul class="nav nav-list">
            <li class="nav-header">Panier</li>
            <li >
                 <a href="{{ path('panier') }}">
                    {% if [listProduits.nom] == 0  %}
                        Aucun article dans votre panier
                    {% elseif [listProduits.nom] == 1 %}
                        1 article dans votre panier
                    {% else %}
                        {{ [listProduits.nom] }} articles dans votre panier
                    {% endif %}
                 </a>
            </li>

        </ul>
    </div>
